[project]
name = "lerobot_policy_act_relative_rtc_2"
version = "0.1.0"
description = "ACT policy with relative joint representations for LeRobot (v2 - single obs, full action prefix)."
requires-python = ">=3.10"
dependencies = [
  # Pinned to commit 6d0d65a (2025-12-28) - latest main, ahead of PyPI 0.4.2
  "lerobot @ git+https://github.com/huggingface/lerobot.git@6d0d65a5fec490c966b186bf9455ed3ea82f3f52",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
